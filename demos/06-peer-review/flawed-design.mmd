classDiagram
    class LoanManager {
        +List~Loan~ loans
        +Map~String, Double~ clientBalances
        +processLoan(loan)
        +validateAvailable(loan)
        +calculateLateFee(loan)
        +updateLoans(loan)
        +printReport()
        +sendEmail(client)
    }

    class Loan {
        +String memberId
        +String bookId
    }

    LoanManager --> Loan : processes
